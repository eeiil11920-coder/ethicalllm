# API keys for wave-2 collection (copy to .env and fill in; .env is never
# committed). collect.py --check verifies each key with a 1-token request.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=

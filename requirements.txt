# Python >= 3.11 (developed and executed on 3.13, Windows)
pandas>=2.0
openpyxl>=3.1
python-docx>=1.1
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.26
scipy>=1.11
statsmodels>=0.14
nltk>=3.8            # VADER lexicon auto-downloaded on first run
spacy>=3.7           # linguistic profiling (optional at runtime)
torch>=2.3           # GPU build recommended (cu12x) for the NLI model
transformers>=4.40   # roberta-large-mnli (auto-downloaded, ~1.4 GB)
openai>=2.0          # OpenAI + xAI + DeepSeek (OpenAI-compatible endpoints)
anthropic>=0.60
google-generativeai>=0.8
